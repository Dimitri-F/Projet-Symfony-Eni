{% extends 'base.html.twig' %}

{% block title %}Enregistrer un utilisateur{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    <h1 class="text-center text-4xl mb-6">Enregistrer un utilisateur</h1>
    <div class="w-1/5 mx-auto">
        {{ form_errors(registrationForm) }}
        {{ form_start(registrationForm) }}
        <div class="mb-3 flex justify-between">
            {{ form_label(registrationForm.pseudo) }}
            {{ form_widget(registrationForm.pseudo) }}
            {{ form_errors(registrationForm.pseudo) }}
        </div>
        <div class="mb-3 flex justify-between">
            {{ form_label(registrationForm.email) }}
            {{ form_widget(registrationForm.email) }}
            {{ form_errors(registrationForm.email) }}
        </div>
        <div class="mb-3 flex justify-between">
            {{ form_label(registrationForm.plainPassword) }}
            {{ form_widget(registrationForm.plainPassword) }}
            {{ form_errors(registrationForm.plainPassword) }}
        </div>
        <div class="mb-3 flex justify-between">
            <label for="lastName">Nom :</label>
            <input class="registrationForm" id="lastName" name="lastName" type="text">
        </div>
        <div class="mb-3 flex justify-between">
            <label for="firstName">Prénom :</label>
            <input class="registrationForm" id="firstName" name="firstName" type="text">
        </div>
        <div class="mb-3 flex justify-between">
            <label for="tel">Téléphone :</label>
            <input class="registrationForm" id="tel" name="tel" type="text">
        </div>
        <div class="mb-3 flex justify-between">
            <label for="site-select">Site :</label>
            <select class="registrationForm" name="site" id="site-select">
                <option value="1">Nantes</option>
                <option value="2">Rennes</option>
                <option value="3">Quimper</option>
                <option value="4">Niort</option>
                <option value="5">Paris</option>
            </select>
        </div>
        <div class="mb-3 flex justify-evenly">
            {{ form_label(registrationForm.agreeTerms) }}
            {{ form_widget(registrationForm.agreeTerms) }}
            {{ form_errors(registrationForm.agreeTerms) }}
        </div>
        <div class="mb-3 flex justify-center">
            <button type="submit" class="btn">Valider</button>
        </div>
        {{ form_end(registrationForm) }}
    </div>

        {# affichage msg d'erreur #}
{#        {% if app.flashes('error') is not empty %}#}
{#            {% for messages in app.flashes('error') %}#}
{#                <div class="alert alert-danger w-2/5 mx-auto flex justify-center items-center">#}
{#                    <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">#}
{#                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />#}
{#                    </svg>#}
{#                    <span>{{ messages }}</span>#}
{#                </div>#}
{#            {% endfor %}#}
{#        {% endif %}#}
    </div>
    <div class="mt-3">
        {{ form_start(registrationCSV) }}
        <div class="w-1/5 mx-auto my-16">
            <div class="mb-3 flex flex-col text-center">
                <label for="fileInput" class="form-label text-2xl">Ou choisir un fichier CSV</label>
                <input type="file" class="form-control" id="fileInput" name="csvFile">
            </div>
            <div class="mb-3 flex justify-center">
                <button type="submit" class="btn">Valider</button>
            </div>
        </div>
        {{ form_end(registrationCSV) }}
    </div>

{% endblock %}
